

const AssigantureGradeSelector = () => {
  return (
    <div></div>
    // <select
    //     className={`min-w-[160px] max-w-[160px] min-h-[46px] max-h-[46px] text-center font-semibold cursor-pointer outline-none transition-all duration-300 
    //         ${student.area_grades[0] ? "border-green-500 bg-green-100 dark:bg-green-900 dark:border-green-300" : 
    //         "border-yellow-500 bg-yellow-100 dark:bg-yellow-900 dark:border-yellow-300"}
    //     `}
    //     value={student.area_grades[0] ? student.area_grades[0].calification : 'NA'}
    //     onChange={() => {}}
    //     // onChange={e => setAverageGrade(e.target.value)}
    //     >
    //     {gradeOptions.map((grade) => (
    //         <option key={grade} value={grade}>
    //         {grade}
    //         </option>
    //     ))}
    // </select>
  )
}

export default AssigantureGradeSelector